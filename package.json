{
  "name": "yc-job-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:convex": "npx convex dev",
    "dev": "concurrently \"npm run dev:convex\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build",
    "deploy": "npx convex deploy"
  },
  "dependencies": {
    "convex": "^1.41.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
